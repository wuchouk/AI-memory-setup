# OpenMemory API Environment Variables
# Copy this to ~/mem0/openmemory/api/.env

# Your username — used as the default user_id for memories
USER=your-username

# OpenAI API key — set to 'not-needed' when using Ollama
OPENAI_API_KEY=not-needed

# Ollama host — Docker uses host.docker.internal to reach host services
OLLAMA_HOST=http://host.docker.internal:11434
